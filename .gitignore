# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.production

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
# Unanchored so it matches at any depth: the rule used to be android/app-only
# (and duplicated), which left a copy at the repo root untracked but committable
# by `git add -A`. It carries Firebase project config and API keys.
google-services.json

# Android Gradle build outputs (never commit)
android/app/build/
android/.gradle/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Pulled APKs for on-device inspection — build artifacts, not source.
device-apk/
