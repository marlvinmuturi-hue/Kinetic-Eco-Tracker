rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // User profile photos: users/{userId}/profile_photo.jpg
    match /users/{userId}/profile_photo.jpg {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
